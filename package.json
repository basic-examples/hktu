{
  "private": true,
  "name": "hktu",
  "version": "0.3.0",
  "description": "HKT Utils",
  "repository": {
    "type": "git",
    "url": "https://github.com/basic-examples/hktu.git"
  },
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json && cpy README.md dist && package-json-minifier",
    "publish": "npm run build && cd dist && npm publish"
  },
  "devDependencies": {
    "cpy-cli": "^6.0.0",
    "package.json-minifier": "^0.0.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2"
  }
}
